/* Used to build canonical URLs, Open Graph tags, and sitemap.xml entries.
   Using the bare domain (no www) as given — flag if you actually want the
   www subdomain instead. */
export const SITE_URL = 'https://roarchitects.co';
export const SITE_NAME = 'ROAR Architects';
